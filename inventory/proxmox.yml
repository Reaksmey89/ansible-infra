plugin: community.general.proxmox
url: https://10.13.1.236:8006
user: ansible@pve
token_id: semaphore
token_secret: "<secret_du_token>"
validate_certs: false
want_facts: true
want_proxmox_nodes_ansible_host: true
groups:
  running: item.status == 'running'
    stopped: item.status == 'stopped'
